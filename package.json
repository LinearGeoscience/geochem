{
    "name": "geochem-pro-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm:backend\" \"npm:frontend\"",
        "backend": "cd backend && uvicorn app.main:app --reload",
        "frontend": "cd frontend && npm run dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "react-colorful": "^5.6.1"
    }
}
